apiVersion: v1
kind: ConfigMap
metadata:
  name: kova-config
  labels:
    app: kova
data:
  kova.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.kova/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Kova Assistant",
            "workspace": "~/.kova/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Kova Assistant

    You are a helpful AI assistant running in Kubernetes.
