apiVersion: v1
kind: Service
metadata:
  name: kova
  labels:
    app: kova
spec:
  type: ClusterIP
  selector:
    app: kova
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
