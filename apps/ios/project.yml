name: Kova
options:
  bundleIdPrefix: ai.kova
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    ENABLE_APP_INTENTS_METADATA_GENERATION: NO

packages:
  KovaKit:
    path: ../shared/KovaKit
  Swabble:
    path: ../../Swabble

schemes:
  Kova:
    shared: true
    build:
      targets:
        Kova: all
    test:
      targets:
        - KovaTests
        - KovaLogicTests
  KovaLogicTests:
    shared: true
    build:
      targets:
        KovaLogicTests: all
    test:
      targets:
        - KovaLogicTests

targets:
  Kova:
    type: application
    platform: iOS
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
    sources:
      - path: Sources
    dependencies:
      - target: KovaShareExtension
        embed: true
      - target: KovaActivityWidget
        embed: true
      - target: KovaWatchApp
      - package: KovaKit
      - package: KovaKit
        product: KovaChatUI
      - package: KovaKit
        product: KovaProtocol
      - package: Swabble
        product: SwabbleKit
      - sdk: AppIntents.framework
    preBuildScripts:
      - name: SwiftFormat (lint)
        basedOnDependencyAnalysis: false
        inputFileLists:
          - $(SRCROOT)/SwiftSources.input.xcfilelist
        script: |
          set -euo pipefail
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          if ! command -v swiftformat >/dev/null 2>&1; then
            echo "error: swiftformat not found (brew install swiftformat)" >&2
            exit 1
          fi
          swiftformat --lint --config "$SRCROOT/../../.swiftformat" \
            --filelist "$SRCROOT/SwiftSources.input.xcfilelist"
      - name: SwiftLint
        basedOnDependencyAnalysis: false
        inputFileLists:
          - $(SRCROOT)/SwiftSources.input.xcfilelist
        script: |
          set -euo pipefail
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          if ! command -v swiftlint >/dev/null 2>&1; then
            echo "error: swiftlint not found (brew install swiftlint)" >&2
            exit 1
          fi
          swiftlint lint --config "$SRCROOT/.swiftlint.yml" --use-script-input-file-lists
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_ENTITLEMENTS: Sources/Kova.entitlements
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        PRODUCT_BUNDLE_IDENTIFIER: "$(KOVA_APP_BUNDLE_ID)"
        PROVISIONING_PROFILE_SPECIFIER: "$(KOVA_APP_PROFILE)"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        SUPPORTS_LIVE_ACTIVITIES: YES
        ENABLE_APPINTENTS_METADATA: NO
        ENABLE_APP_INTENTS_METADATA_GENERATION: NO
      configs:
        Debug:
          KOVA_PUSH_TRANSPORT: direct
          KOVA_PUSH_DISTRIBUTION: local
          KOVA_PUSH_RELAY_BASE_URL: ""
          KOVA_PUSH_APNS_ENVIRONMENT: sandbox
        Release:
          KOVA_PUSH_TRANSPORT: direct
          KOVA_PUSH_DISTRIBUTION: local
          KOVA_PUSH_RELAY_BASE_URL: ""
          KOVA_PUSH_APNS_ENVIRONMENT: production
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: Kova
        CFBundleIconName: AppIcon
        CFBundleURLTypes:
          - CFBundleURLName: ai.kova.ios
            CFBundleURLSchemes:
              - kova
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        KovaCanonicalVersion: "$(KOVA_IOS_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIBackgroundModes:
          - audio
          - remote-notification
        BGTaskSchedulerPermittedIdentifiers:
          - ai.kova.ios.bgrefresh
        NSLocalNetworkUsageDescription: Kova discovers and connects to your Kova gateway on the local network.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoadsInWebContent: true
        NSBonjourServices:
          - _kova-gw._tcp
        NSCameraUsageDescription: Kova can capture photos or short video clips when requested via the gateway.
        NSLocationWhenInUseUsageDescription: Kova uses your location when you allow location sharing.
        NSLocationAlwaysAndWhenInUseUsageDescription: Kova can share your location in the background when you enable Always.
        NSMicrophoneUsageDescription: Kova needs microphone access for voice wake.
        NSMotionUsageDescription: Kova may use motion data to support device-aware interactions and automations.
        NSPhotoLibraryUsageDescription: Kova needs photo library access when you choose existing photos to share with your assistant.
        NSSpeechRecognitionUsageDescription: Kova uses on-device speech recognition for voice wake.
        NSSupportsLiveActivities: true
        ITSAppUsesNonExemptEncryption: false
        KovaPushTransport: "$(KOVA_PUSH_TRANSPORT)"
        KovaPushDistribution: "$(KOVA_PUSH_DISTRIBUTION)"
        KovaPushRelayBaseURL: "$(KOVA_PUSH_RELAY_BASE_URL)"
        KovaPushAPNsEnvironment: "$(KOVA_PUSH_APNS_ENVIRONMENT)"
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  KovaShareExtension:
    type: app-extension
    platform: iOS
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
    sources:
      - path: ShareExtension
    dependencies:
      - package: KovaKit
      - sdk: AppIntents.framework
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        ENABLE_APPINTENTS_METADATA: NO
        ENABLE_APP_INTENTS_METADATA_GENERATION: NO
        PRODUCT_BUNDLE_IDENTIFIER: "$(KOVA_SHARE_BUNDLE_ID)"
        PROVISIONING_PROFILE_SPECIFIER: "$(KOVA_SHARE_PROFILE)"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
    info:
      path: ShareExtension/Info.plist
      properties:
        CFBundleDisplayName: Kova Share
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.share-services
          NSExtensionPrincipalClass: "$(PRODUCT_MODULE_NAME).ShareViewController"
          NSExtensionAttributes:
            NSExtensionActivationRule:
              NSExtensionActivationSupportsText: true
              NSExtensionActivationSupportsWebURLWithMaxCount: 1
              NSExtensionActivationSupportsImageWithMaxCount: 10
              NSExtensionActivationSupportsMovieWithMaxCount: 1

  KovaActivityWidget:
    type: app-extension
    platform: iOS
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
    sources:
      - path: ActivityWidget
      - path: Sources/LiveActivity/KovaActivityAttributes.swift
    dependencies:
      - sdk: WidgetKit.framework
      - sdk: ActivityKit.framework
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        PRODUCT_BUNDLE_IDENTIFIER: "$(KOVA_ACTIVITY_WIDGET_BUNDLE_ID)"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        SUPPORTS_LIVE_ACTIVITIES: YES
    info:
      path: ActivityWidget/Info.plist
      properties:
        CFBundleDisplayName: Kova Activity
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
        NSSupportsLiveActivities: true
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension

  KovaWatchApp:
    type: application.watchapp2
    platform: watchOS
    deploymentTarget: "11.0"
    sources:
      - path: WatchApp
    dependencies:
      - target: KovaWatchExtension
    configFiles:
      Debug: Config/Signing.xcconfig
      Release: Config/Signing.xcconfig
    attributes:
      DevelopmentTeam: "$(KOVA_DEVELOPMENT_TEAM)"
      ProvisioningStyle: "$(KOVA_CODE_SIGN_STYLE)"
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        ENABLE_APPINTENTS_METADATA: NO
        ENABLE_APP_INTENTS_METADATA_GENERATION: NO
        PRODUCT_BUNDLE_IDENTIFIER: "$(KOVA_WATCH_APP_BUNDLE_ID)"
        PROVISIONING_PROFILE_SPECIFIER: "$(KOVA_WATCH_APP_PROFILE)"
    info:
      path: WatchApp/Info.plist
      properties:
        CFBundleDisplayName: Kova
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
        WKCompanionAppBundleIdentifier: "$(KOVA_APP_BUNDLE_ID)"
        WKWatchKitApp: true

  KovaWatchExtension:
    type: watchkit2-extension
    platform: watchOS
    deploymentTarget: "11.0"
    sources:
      - path: WatchExtension/Sources
    dependencies:
      - sdk: AppIntents.framework
      - sdk: WatchConnectivity.framework
      - sdk: UserNotifications.framework
    configFiles:
      Debug: Config/Signing.xcconfig
      Release: Config/Signing.xcconfig
    attributes:
      DevelopmentTeam: "$(KOVA_DEVELOPMENT_TEAM)"
      ProvisioningStyle: "$(KOVA_CODE_SIGN_STYLE)"
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        PRODUCT_BUNDLE_IDENTIFIER: "$(KOVA_WATCH_EXTENSION_BUNDLE_ID)"
        PROVISIONING_PROFILE_SPECIFIER: "$(KOVA_WATCH_EXTENSION_PROFILE)"
    info:
      path: WatchExtension/Info.plist
      properties:
        CFBundleDisplayName: Kova
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
        NSExtension:
          NSExtensionAttributes:
            WKAppBundleIdentifier: "$(KOVA_WATCH_APP_BUNDLE_ID)"
          NSExtensionPointIdentifier: com.apple.watchkit

  KovaTests:
    type: bundle.unit-test
    platform: iOS
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
    sources:
      - path: Tests
        excludes:
          - Logic
    dependencies:
      - target: Kova
      - package: Swabble
        product: SwabbleKit
      - sdk: AppIntents.framework
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        PRODUCT_BUNDLE_IDENTIFIER: ai.kova.ios.tests
        ENABLE_APP_INTENTS_METADATA_GENERATION: NO
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Kova.app/Kova"
        BUNDLE_LOADER: "$(TEST_HOST)"
    info:
      path: Tests/Info.plist
      properties:
        CFBundleDisplayName: KovaTests
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"

  KovaLogicTests:
    type: bundle.unit-test
    platform: iOS
    configFiles:
      Debug: Signing.xcconfig
      Release: Signing.xcconfig
    sources:
      - path: Tests/Logic
    dependencies:
      - package: KovaKit
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: "$(KOVA_CODE_SIGN_STYLE)"
        DEVELOPMENT_TEAM: "$(KOVA_DEVELOPMENT_TEAM)"
        PRODUCT_BUNDLE_IDENTIFIER: ai.kova.ios.logic-tests
        ENABLE_APP_INTENTS_METADATA_GENERATION: NO
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
    info:
      path: Tests/Info.plist
      properties:
        CFBundleDisplayName: KovaLogicTests
        CFBundleShortVersionString: "$(KOVA_MARKETING_VERSION)"
        CFBundleVersion: "$(KOVA_BUILD_VERSION)"
