import Foundation
import KovaKit

// Prefer the KovaKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = KovaKit.AnyCodable
typealias InstanceIdentity = KovaKit.InstanceIdentity
