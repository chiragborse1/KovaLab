---
summary: "CLI reference for `kova kova` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `kova kova ...`
  - You need migration guidance to current commands
title: "Kova"
---

# `kova kova`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `kova kova qr` (same behavior as [`kova qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `kova kova qr` -> `kova qr`

## Related

- [CLI reference](/cli)
