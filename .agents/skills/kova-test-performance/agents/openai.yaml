interface:
  display_name: "Kova Test Performance"
  short_description: "Benchmark and fix slow Kova tests"
  default_prompt: "Use $kova-test-performance to reassess the Kova test benchmark, identify the next real hotspot, fix it without losing coverage, update the report, and commit scoped changes."
policy:
  allow_implicit_invocation: false
